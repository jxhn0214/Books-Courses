def main():
    glossary = {
        'list': 'a list of elements', 
        'tuple': 'a non-dynamic list',
        'dictionary': 'a list that holds key-values', 
        'conditional': 'a statement that returns True or False', 
        'for loop': 'a loop composed of intializer, conditon, iterator'
    }

    print(f"A list is {glossary['list']}")
    print(f"A tuple is {glossary['tuple']}")
    print(f"A dictionary is {glossary['dictionary']}")
    print(f"a conditional is {glossary['conditional']}")
    print(f"a for loop is {glossary['for loop']}")

main()